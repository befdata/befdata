- content_for :actions do
  = link_to datagroups_path do
    = image_tag "list-ordered.png"
    List Datagroups

%h2
  Categories
= render :partial => 'categories_table', :object => @categories, :locals => {:with_datagroups => true}