- content_for :actions do
  = link_to datagroups_path do
    = image_tag "list-ordered.png"
    List Datagroups
  = link_to categories_path do
    = image_tag "list-ordered.png"
    List Categories


%h2= "Category: #{@category.short}"
%h4 Long
%p= @category.long
%h4 Description
%p= @category.description
%h4 Comment
%p= @category.comment
- unless @category.datagroup.blank?
  %h4 Datagroup
  %p= link_to @category.datagroup.title, @category.datagroup


-#- unless categories.datacolumn.empty?
-#  %h3 Datasets
-#  %ul
-#    - @datagroup.datacolumns.collect{|dc| dc.dataset}.uniq.sort_by(&:title).each do |ds|
-#      %li
-#        = render :partial => ds, :locals => {:with_cart => true, :with_comment => false}

-#- unless @datagroup.categories.empty?
-#  %h3 Categories
-#  %ul
-#    - @datagroup.categories.sort_by(&:short).each do |cat|
-#      %li
-#        = link_to cat.short, cat