- @data_groups_available.each do |m|
  %ul
    %li
      = m.title
      \:
      \#{truncate(m.description, :length => 200,  :omission => '... (continued)')}
      - m.datacolumns.each do |sm|
        %ul
          %li
            = sm.columnheader
            (#{truncate(sm.definition, :length => 100, :omission => '... (continued)')})