%h2 Paper Proposals #{link_to(image_tag("add.gif"), :new_paperproposal)}

- @paperproposals.each do |pp|
  %div.box
    %h3
      = pp.title
      = link_to image_tag("page_go.png"), paperproposal_path(pp)
      - if pp.lock
        = image_tag("lock.png")
      - else
        = link_to image_tag("pencil.png"), edit_paperproposal_path(pp)
        = image_tag("lock_open.png")
      = link_to image_tag("cross.png"), pp,
                                      :confirm => "Are you sure you want delete this data proposal?",
                                      :method => :delete

    %h4= "#{pp.author} created at: #{pp.created_at.strftime("%Y-%m-%d")}"
    %ul
      - pp.author_list[:author_list].each do |a|
        %li= a.to_s
    Rationale:
    %p
      = pp.rationale

    - unless pp.freeformats.blank?
      - @freeformats = pp.freeformats
      = render :partial => 'freeformats/index'

